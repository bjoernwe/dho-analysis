# Disable Kotlin incremental compilation: the kotlinx-dataframe compiler plugin
# emits a synthetic "__GENERATED DECLARATIONS__.kt" with a relative path, which
# crashes the incremental cache close (RelocatableFileToPathConverter expects an
# absolute path) and forces a non-daemon fallback compile.
kotlin.incremental=false
